syntax = "proto3";
package lattice;

message Atom {
    float x = 1;
    float y = 2;
    float z = 3;
    float radius = 4;
    string symbol = 5;
}

message Lattice {
    repeated Atom atoms = 1;
    map<string, string> colors = 2;
}